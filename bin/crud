#! /usr/bin/env node

const program = require('commander')

program
    .version('0.0.1', '-v', '--version')
    .command('create [path,name]', 'create module')
    // .command('refresh', 'refresh routers....')
program.parse(process.argv)
